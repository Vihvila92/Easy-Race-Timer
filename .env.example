# Example environment configuration (placeholder – adjust when code added)

# Deployment mode: standalone | self-hosted | managed-cloud
DEPLOYMENT_MODE=standalone

# PostgreSQL connection (Docker network hostnames assumed)
DATABASE_URL=postgres://app_user:app_password@postgres:5432/easyracetimer

# JWT secrets (use long random strings in real deployments)
JWT_ACCESS_SECRET=replace-with-long-random-string
JWT_REFRESH_SECRET=replace-with-long-random-string

# Time synchronization endpoint signing secret
TIME_SYNC_SECRET=replace-with-random

# Feature flags (comma separated, optional)
FEATURE_FLAGS=

# Logging: debug | info | warn | error
LOG_LEVEL=info

# Backup storage (optional)
BACKUP_CLOUD_STORAGE=

# Email contact for security
SECURITY_CONTACT=valtteri@vehvilainen.cc

# Default organization ID (standalone mode)
DEFAULT_ORGANIZATION_ID=local-org
